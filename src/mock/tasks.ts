import { Document, RescanRecord, Task } from '@/types';

/**
 * 演示环境以空业务数据启动。
 * 任务关系库和角色配置属于系统配置，不放在这里清空。
 */
export const mockTasks: Task[] = [];
export const mockDocuments: Document[] = [];
export const mockRescanRecords: RescanRecord[] = [];
