# 小助理作业管理平台｜内网复用与开发交接说明

> 更新日期：2026-09-03
> 本轮范围：2026-09-02 至 2026-09-03 的需求开发与收口。由于本轮尚未形成中间 Git 提交，本清单以当前工作区相对提交 `105b371` 的代码差异、已确认需求和现有构建结果为准。

## 1. 交付包与运行方式

- `src/`：React + TypeScript 前端源码。
- `server/`：独立平台 API 路由契约和 1d 适配器说明；当前业务接口为待实现契约。
- `database/schema.sql`：OneDay Cloud 数据库基础表及本轮扩展字段。
- `dist/`、`bundle.html`：构建预览产物。
- `package.json`、`package-lock.json`：依赖与启动命令。

不包含 `node_modules`、真实台账、真实上传文件、账号、Cookie、Token、`.env` 或任何密钥。

```bash
npm install
npm run dev
```

前端默认地址：`http://localhost:3015`。生产构建命令：`npm run build`。

当前 `src/services/db.ts` 中 `USE_MOCK=true`，因此这是“空数据流程 Demo”，不是已经接通 OneDay Cloud 的生产系统。所有业务测试任务、测试交付物、测试回扫记录和测试人员名录已清空；保留任务关系库、角色定义、页面与接口契约。

## 2. 本轮开发改动总览

### 2.1 三角色工作台与导航收口

登录身份统一为“管理员、组长、组员”，演示身份不再按具体人名展示。左侧菜单按角色收敛：

- 管理员：全局总览、任务全景、审核中心、人员明细、数据导入、组织与配置。
- 组长：本组工作台、本组任务、审核中心、本组人员、导入中心。
- 组员：我的工作台、我的任务、工作记录、我的交付物。

当前身份切换仅用于前端演示。正式环境必须从登录会话读取真实用户、角色和小组，不能信任前端传参。

### 2.2 任务进入与字段完善

任务支持两种进入方式：

1. Excel 台账批量导入：入库后先进入“待完善”，并分配给对应组长；组长补齐必要字段后转为“进行中”。
2. 组长手动创建：一次填写完整任务字段并直接进入“进行中”。

组长可维护的核心字段包括：任务名称、任务归属、主任务、任务分组、作业性质、任务类型、负责人、小组、组长、数据报告人、验收人、数据量级、作业人力、预计截止时间、实际截止时间、平台任务 ID、规则文档链接、备注和下发难度。

“预计截止时间”是组长预填的预计完成时间；“实际截止时间”是业务实际要求的截止时间。两者均使用日历选择。任务归属、主任务、任务分组采用级联关系，既可选择关系库已有项，也可填写本任务专用的自定义值。

### 2.3 三级任务挂链与分组查看

任务统一按以下结构组织：

```text
任务归属
└── 主任务
    └── 任务分组
        └── 具体任务名称
```

无主任务从属关系的任务统一归为“临时任务”。已录入关系库：

- AI试穿-模型评测：66 个任务分组。
- lookie横向评测：10 个任务分组，其中评测集标注 2 个、效果评测 6 个、临时任务 2 个。

管理员可从“任务全景”查看全部任务；组长按本组查看；组员只查看本人负责或参与的任务。管理员任务全景表头顺序为：任务归属、主任务、任务分组、任务名称、状态、负责人、小组、预计截止时间，其后补充实际截止时间、进度和难度。

任务关系管理支持新增、查询、修改和逻辑停用。组长负责把需要长期复用的自定义关系正式加入关系库；历史任务保存快照，不随关系库改名或停用而丢失。

### 2.4 成员和工作记录

- 组长可新增、编辑、停用和调整本组组员；管理员可查看和维护全局人员。
- 组员可登记或移除自己的工作标签；组长可以代登记、确认或移除本组记录。
- 工作标签支持多选挂载，说明和证据默认选填；“其他已验收交付物”必须填写备注。
- 文档类标签可关联已验收文档，回扫和验收类标签可关联对应业务记录。
- 只有组长确认后的工作标签才进入人员统计和任务结项依据。
- 人员作业明细支持日、周、月三个时间维度。

标准标签：规则文档撰写、需求文档撰写、需求文档培训、规则培训、过程答疑、作业中规则变更并下发、回扫安排/发起、数据验收、数据分析、评测报告撰写、其他已验收交付物、组员带教。

### 2.5 组长审核中心

组长审核中心包含：待完善项目、我的进行中、交付物待确认、待处理返修、我的待办、人员动态。

- 台账导入但字段不完整的任务进入“待完善项目”。
- 组员提交的交付物进入“交付物待确认”和“我的待办”。
- 管理员驳回的交付物进入“待处理返修”。
- 人员动态按成员汇总进行中任务、待完善字段和待确认交付物。

### 2.6 管理员审核中心

管理员审核中心只处理组长提交的二级验收交付物，不承担任务字段完善。

- 待办只保留“交付物审核”和“返修跟进”。
- 支持按小组查看交付物验收及返修进度。
- 展示管理员累计驳回次数、当前返修所在环节、最近意见和提交时间。
- 管理员只能操作“待管理员验收”的交付物；已驳回记录只能查看进度，必须由组长处理后重新提交。

### 2.7 交付物动态双路线闭环

组员提交后由组长首次验收，组长三选一：

```text
组员提交
├── 组长驳回 → 组员上传新版本 → 重新进入组长验收
├── 组长通过并结束 → 最终完成（仅组长验收）
└── 组长通过并提交管理员
    ├── 管理员通过 → 最终完成（管理员验收）
    └── 管理员驳回 → 回到组长
        ├── 组长直接修改 → 重提管理员
        └── 组长退回组员 → 组员上传新版本 → 组长复核 → 重提管理员
```

关键约束：

- 是否需要管理员验收由组长首次审核时动态决定。
- 一旦进入“组长 → 管理员”路线，当前及后续返修版本不得降级为“仅组长验收”。
- 同类交付物返修会递增版本号，并通过根文档 ID 串联跨版本历史。
- 分别统计组长驳回次数和管理员驳回次数。
- 只有最新版本处于“组长验收完成”或“管理员验收完成”时，才计入文档齐套率和结项依据。
- 每次提交、通过、驳回、退回和重提都写入审核事件记录。

### 2.8 结项与留痕

- 组长在结项时最终确认 1–5 星难度；与下发难度不同时必须填写原因。
- 组长按实际参与环节给成员挂载并确认工作标签，不使用主观贡献分打分。
- 任务字段修改保存修改前值、修改后值、修改人和时间。
- 结项保存最终星级、调整原因、人员标签、确认人、确认时间和结项摘要。

### 2.9 空数据初始化

本轮已移除全部演示任务、演示交付物、演示回扫记录及演示人员姓名。管理员、组长、组员角色和任务关系库仍保留。内网首次运行时应先创建真实小组及成员，再导入台账或手动创建任务。

## 3. 本轮新增或调整的数据字段

### 3.1 任务 `tasks`

| 业务字段 | 数据库字段 | 说明 |
|---|---|---|
| 预计截止时间 | `expected_deadline` | 组长预填的预估完成时间 |
| 实际截止时间 | `deadline` | 业务实际要求的截止时间；待完善阶段允许为空 |
| 任务关系 ID | `relation_id` | 关联任务关系库 |
| 主任务快照 | `main_task_snapshot` | 防止关系库变化影响历史任务 |
| 任务分组快照 | `linked_task_snapshot` | 防止关系库变化影响历史任务 |
| 参与人 | 前端 `participantNames`；内网需规范化存储 | 支持一个任务多人参与 |
| 数据来源 | `source_type` | `manual`、`excel`、`scheduler` |
| 外部任务 ID | `external_task_id` | 对接大系统时的幂等业务键 |
| 导入批次 | `imported_batch_id` | 追溯 Excel 导入来源 |

任务归属当前复用原字段 `ownership`，任务分组当前复用原字段 `task_group`，同时使用关系快照字段保留层级语义。

### 3.2 任务关系 `task_relations`

`ownership`、`main_task`、`linked_task`、`active`、`created_at`、`updated_at`。无主任务从属关系时 `main_task='-'`，前端显示“临时任务”。

### 3.3 任务字段留痕 `task_field_changes`

`task_id`、`field`、`before_value`、`after_value`、`changed_by`、`changed_at`。

### 3.4 交付物 `documents`

| 字段 | 用途 |
|---|---|
| `version`、`replaced_document_id`、`root_document_id` | 串联版本和完整返修链 |
| `status` | 兼容原组长审核状态 |
| `reviewed_by`、`reviewed_at`、`review_comment` | 组长审核信息 |
| `review_route` | `undecided`、`leader_only`、`leader_then_admin` |
| `workflow_status` | 当前唯一流程状态 |
| `leader_rejection_count` | 组长累计驳回次数 |
| `admin_review_status` | 管理员审核兼容状态 |
| `submitted_to_admin_at` | 最近提交管理员时间 |
| `admin_reviewed_by`、`admin_reviewed_at`、`admin_review_comment` | 管理员审核信息 |
| `admin_revision_count` | 管理员累计驳回次数 |
| `final_approval_level` | 最终由组长或管理员验收 |
| `completed_by`、`completed_at` | 最终验收人和时间 |

`workflow_status` 允许值：

- `pending_leader_review`：待组长验收。
- `member_revision_required`：待组员返修。
- `pending_admin_review`：待管理员验收。
- `leader_revision_required`：管理员驳回，待组长处理。
- `completed_by_leader`：仅组长验收完成。
- `completed_by_admin`：管理员最终验收完成。

### 3.5 审核事件 `document_review_events`

记录 `document_id`、`root_document_id`、`task_id`、`actor`、`actor_role`、`action`、`from_status`、`to_status`、`comment`、`created_at`。用于生成跨版本、跨角色审核时间线。

### 3.6 人员工作标签 `task_contributions`

新增 `status`、`confirmed_by`、`confirmed_at`，区分成员自报、组长确认和移除状态。

## 4. 页面与路由清单

| 路由 | 页面 | 主要角色 |
|---|---|---|
| `#/overview` | 全局/本组/个人工作台 | 三角色按身份展示 |
| `#/tasks/register` | 任务全景、本组任务、我的任务 | 三角色 |
| `#/tasks/:id` | 任务全生命周期详情 | 三角色 |
| `#/management-ledger` | 组长或管理员审核中心 | 组长、管理员 |
| `#/my-deliverables` | 我的交付物与返修 | 组员 |
| `#/personal-work` | 人员作业明细/工作记录 | 三角色 |
| `#/members` | 成员与小组管理 | 管理员、组长 |
| `#/task-relations` | 任务关系管理 | 管理员配置入口；组长具备业务维护能力 |
| `#/ledger-import` | 台账导入 | 管理员、组长 |
| `#/team-tasks` | 按组、主任务和任务分组查看 | 管理视角 |

旧的时间任务看板、任务流转、负荷、回扫和系统监测页面仍保留在路由层，主导航已按角色收敛，避免入口冗余。

## 5. 1d / OneDay Cloud 必做接入

先在测试库审核并执行 `database/schema.sql`，再实现 `server/adapters/` 中的适配器。所有待实现路由列在 `server/apiContracts.js`；当前返回 501 是预期行为。

### 5.1 登录与授权

- 对接阿里钉 / OneDay Auth。
- 后端从会话查询 `app_users`、`user_roles`、`team_memberships`。
- 所有写接口必须在服务端校验角色、所属小组和资源归属，不能信任前端传入的姓名、角色、小组、上传人或审核人。

### 5.2 数据仓储

- 将任务、关系、成员、交付物、审核事件、工作标签、回扫、结项和字段变更服务从内存数组改为 OneDay Cloud 数据库读写。
- 任务关系和成员的“删除”采用停用或有效期结束，不能物理删除历史引用。
- 交付物状态迁移和审核事件必须放在同一数据库事务中，并使用版本号或行锁避免重复审核。
- 服务端必须拒绝非法状态跳转，尤其禁止 `leader_then_admin` 降级为 `leader_only`。

### 5.3 文件存储

- `/documents/upload-intents` 申请预签名地址或对象键。
- 浏览器直传对象存储，再调用 `POST /documents` 保存元数据。
- 不能沿用 Demo 中的 `URL.createObjectURL()`；该地址刷新后失效，不能作为正式文件链接。

### 5.4 Excel 台账

- 任务台账预览、字段映射、错误行、待匹配行和提交入库必须落入 `import_batches`、`import_rows`、`task_match_queue`。
- 导入提交需幂等，重复上传不得重复创建任务。
- 需要用公司最新版本的两份台账做字段回归测试。

### 5.5 大系统和作业平台

- 大系统通过 `/api/v1/integration/*` 写入任务与进度。
- 使用 `external_task_id + Idempotency-Key` 幂等去重。
- 外部系统只能更新任务基础字段和进度，不能覆盖交付物、人工审核、回扫、人员贡献和结项记录。
- 作业平台任务 ID 的实时进度拉取目前只有字段与接口位置，尚未接入真实平台。

### 5.6 钉钉通知

服务端实现任务待完善、临期、逾期、交付物驳回、待审核和返修重提通知。凭据只能保存在内网服务端，前端不得接收 access token 或 webhook secret。

## 6. 当前检查结果与卡点

### 6.1 已通过

- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `git diff --check`：通过。
- 本地 `http://localhost:3015`：HTTP 200。
- 空任务、空人员、空交付物和空回扫数据可以完成编译与构建。

### 6.2 P0：内网正式试用前必须解决

1. **后端尚未实现**：`server/apiContracts.js` 是接口目录，不是业务实现；正式数据目前不会持久化。
2. **登录与权限尚未接通**：角色切换是前端演示，不能作为安全边界。
3. **文件没有正式存储**：当前上传链接是浏览器临时 URL，刷新后失效。
4. **审核状态机缺少服务端事务保护**：前端规则已闭环，但必须在后端再次校验权限、当前状态、路线锁定和重复提交。
5. **数据库脚本尚未在真实 OneDay Cloud 测试库执行**：需确认字段约束、自引用外键、RLS、既有表升级和策略命名冲突。
6. **Excel 导入尚未完成正式入库**：当前可解析和预览，但提交、错误队列、幂等与审计需由 1d 实现。

### 6.3 P1：上线范围确认或后续接入

1. **小组数量口径冲突**：早期需求和现有常量是 A/B/C/D 四组；近期需求描述为“三个组”。内网建组织数据前必须确认最终小组清单。
2. **任务状态口径需要再确认**：代码保留 `数据完成、待交付、待验收` 等历史内部状态，部分页面展示口径曾调整。建议在后端落库前确认唯一状态字典和允许迁移表。
3. **任务“多人参与”的存储尚未规范化**：前端有 `participantNames`，正式库建议使用任务参与人关联表，不建议使用字符串数组作为权限依据。
4. **任务关系维护权限需统一**：业务共识为组长负责新增长期复用关系；当前管理员导航中有固定配置入口，组长可通过任务内自定义值使用，但是否在组长主导航直接开放关系管理需最终确认。
5. **实时进度、钉钉通知和大系统同步未接入**。
6. **尚无自动化流程测试**：目前只有类型检查和生产构建；建议为任务导入、三级级联、双路线审核、跨版本返修和权限越权补接口测试与端到端测试。
7. **本轮没有独立 Git 提交基线**：建议评审确认后立即提交并打版本标签，后续需求按单独提交推进，便于回滚和审计。

## 7. 建议的内网实施顺序

1. 确认小组数量、任务状态字典、组长维护任务关系的入口范围。
2. 在 OneDay Cloud 测试库拆分并验证数据库迁移，不直接在生产库执行整份脚本。
3. 接入阿里钉登录和服务端 RBAC。
4. 实现任务、关系、成员和字段留痕仓储。
5. 接入对象存储，跑通组员上传文件。
6. 实现交付物双路线状态机、事务和审核事件。
7. 实现 Excel 批量导入、错误队列和组长待完善提醒。
8. 接入作业平台进度、大系统同步和钉钉通知。
9. 使用管理员、组长、组员三个真实测试账号完成端到端验收。
10. 生产发布前清空测试库，保留任务关系配置并建立备份与回滚方案。

## 8. 建议验收用例

- 台账导入任务 → 分配组长 → 补齐挂链和截止时间 → 自动进入进行中。
- 手动创建任务 → 三级下拉联动 → 自定义关系 → 字段修改留痕。
- 组员提交 → 组长驳回 → 组员 V2 → 组长通过并结束。
- 组员提交 → 组长提交管理员 → 管理员通过并结束。
- 管理员驳回 → 组长直接修改重提 → 管理员通过。
- 管理员驳回 → 组长退回组员 → 组员新版本 → 组长复核 → 管理员通过。
- 已进入管理员路线的返修版本尝试改为仅组长验收 → 服务端拒绝。
- 非本组组长审核、组员操作他人交付物、管理员直接审核未经过组长的交付物 → 服务端拒绝。
- 结项时最终星级变化必须填写原因；未确认的工作标签不计入统计。

## 9. 内网转移与版本管理

```bash
git status
git add .
git commit -m "feat: 完成角色化任务管理与交付物双路线审核"
git tag v1.1-demo
git push origin main --tags
```

如通过离线文件摆渡，压缩源码、`dist/`、`database/schema.sql`、`server/` 和本说明文件；不要包含 `node_modules`、真实 Excel、上传文件、`.env` 或密钥。1d 接手后应先创建功能分支和测试数据库，不要直接在内网生产环境修改。
