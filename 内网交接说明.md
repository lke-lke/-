# 小助理作业管理看板｜内网交接说明

## 1. 本交付包包含什么

- `src/`：React 前端源码（任务、看板、台账导入、结项确认等）。
- `server/`：独立平台 API 契约和未来 1d 适配器说明。
- `database/schema.sql`：OneDay Cloud 正式数据库建表及扩展字段脚本。
- `bundle.html` 与 `dist/`：当前可离线预览的前端构建产物。
- `package.json`、`package-lock.json`：Node 依赖与启动命令。

不含：`node_modules`、真实台账、用户文件、账号、Cookie、Token、`.env` 或任何密钥。

## 2. 当前独立演示模式

当前项目使用本地 mock 数据，目的是先跑通业务流程。启动后访问：

```bash
npm install
npm run dev
```

浏览器打开 `http://localhost:3015`。API 契约服务运行于 `http://localhost:3020`；健康检查为 `/api/health`。

生产构建：

```bash
npm run build
```

## 3. 已实现的业务流程

1. Excel 台账手动导入：识别任务台账与业务借调/回扫台账，预览后确认入库。
2. 四组看板：管理总览、时间任务看板、任务流转、小组负荷与系统建设监测。
3. 文档交付：上传规则文档、需求文档、评测报告等；新上传文档先进入“待验收”。
4. 文档验收：组长可验收或驳回；仅“已验收”文档计入文档齐套及结项依据。
5. 结项确认：仅“待验收”任务可结项；组长确认最终难度星级，若与下发星级不同必须记录原因。
6. 人员工作标签：组长为组员多选挂载实际工作标签，可选关联文档、回扫记录、数据验收；过程说明选填，“其他已验收交付物”备注必填。
7. 结项留痕：保存最终星级、调整原因、人员标签、确认人、确认时间及结项摘要；确认后任务进入“已完成”。

标准人员工作标签：规则文档撰写、需求文档撰写、需求文档培训、规则培训、过程答疑、作业中规则变更并下发、回扫安排/发起、数据验收、数据分析、评测报告撰写、其他已验收交付物、组员带教。

## 4. 1d / OneDay Cloud 接入清单

先执行 `database/schema.sql`。然后由 1d 在 `server/adapters/` 实现以下适配器：

- `authProvider`：接入阿里钉 / OneDay Auth；后端从会话中识别用户、角色及小组，不能信任前端提交的身份信息。
- `repository`：接入 OneDay Cloud 数据库，落实任务、文档、回扫、结项、审计日志读写。
- `storageProvider`：文档先申请上传地址，再写入文档元数据；文件本体不经过前端 mock。
- `schedulerAdapter`：未来大系统使用 `/api/v1/integration/*` 写入任务和进度。以 `external_task_id` + `Idempotency-Key` 幂等去重；不得覆盖文档、验收、回扫和结项记录。

所有待实现 API 已列在 `server/apiContracts.js`，返回 501 是预期行为，表示接口契约已预留、尚未连接内网适配器。

结项相关正式表：`difficulty_revisions`、`documents`/`document_reviews`、`task_contributions`、`task_settlements`、`audit_logs`。

## 5. 权限建议

- 小助理：查看本组任务、上传文档、登记过程信息。
- 组长：验收文档、挂载人员工作标签、最终难度确认、结项。
- 领导/管理者：查看全局看板与已结项归档。
- 管理员：维护账号、小组、角色、标签库和系统对接配置。

## 6. 内网转移方式（无须由 Codex 访问内网）

1. 将本交付包 zip 通过公司允许的文件摆渡、U 盘或受控文件传输带入内网。
2. 在内网电脑解压后创建一个空 Git 仓库，例如 `assistant-operations-dashboard`。
3. 在解压目录执行：

```bash
git init
git add .
git commit -m "feat: 小助理作业管理看板初始交付"
git branch -M main
git remote add origin <内网 Git 仓库地址>
git push -u origin main
```

4. 1d 从该仓库拉取源码，根据第 4 节接入 OneDay Auth、数据库、对象存储和未来调度系统。

`.gitignore` 已排除依赖、构建缓存、环境变量和密钥。请勿提交真实 Excel 台账、用户上传文档或 `.env`。
